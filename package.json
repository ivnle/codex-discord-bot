{
  "name": "codex-discord-bot",
  "version": "0.1.0",
  "description": "Discord gateway for local Codex CLI sessions driven through codex app-server.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "codex-discord-bot": "./dist/main.js"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "discord.js": "^14.19.3",
    "yaml": "^2.7.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3",
    "vitest": "^3.1.3"
  },
  "engines": {
    "node": ">=22"
  }
}
